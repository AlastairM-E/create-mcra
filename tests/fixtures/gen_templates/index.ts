const { initialIndexJsFixture } = require('./initialIndexJsFixture.ts');
const { reactFunctionalTemplateFixture } = require('./reactFunctionalTemplateFixture.ts');
const { reactClassTemplateFixture } = require('./reactClassTemplateFixture.ts');
const { jestTestingTemplateFixture } = require('./jestTestingTemplateFixture.ts');

module.exports = {
  initialIndexJsFixture,
  reactFunctionalTemplateFixture,
  reactClassTemplateFixture,
  jestTestingTemplateFixture,
};
